name: iOS

# Manual trigger only — macOS runners consume 10x minutes on private repos,
# so we build iOS on demand rather than on every push.
on:
  workflow_dispatch:

concurrency:
  group: ios-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ios-unsigned-ipa:
    name: iOS build (unsigned)
    runs-on: macos-15
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      # RN 0.79's pinned fmt pod fails to compile under the newest clang on
      # macos-latest; pin the newest Xcode 16.x, which RN 0.79 supports.
      - name: Select Xcode 16
        run: |
          XCODE_PATH=$(ls -d /Applications/Xcode_16*.app 2>/dev/null | sort -V | tail -1)
          test -n "$XCODE_PATH" || { echo "No Xcode 16 found:"; ls /Applications | grep -i xcode; exit 1; }
          sudo xcode-select -s "$XCODE_PATH/Contents/Developer"
          xcodebuild -version

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install JS dependencies
        run: npm ci

      - name: Install CocoaPods dependencies
        working-directory: ios
        run: |
          if [ -f ../Gemfile ]; then
            bundle install
            bundle exec pod install
          else
            pod install
          fi

      - name: Build (Release, device, no code signing)
        working-directory: ios
        run: |
          xcodebuild \
            -workspace Lumina.xcworkspace \
            -scheme Lumina \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            build

      - name: Package unsigned .ipa
        working-directory: ios
        run: |
          APP_PATH="build/Build/Products/Release-iphoneos/Lumina.app"
          test -d "$APP_PATH"
          mkdir -p Payload
          cp -R "$APP_PATH" Payload/
          zip -qry Lumina-unsigned.ipa Payload
          ls -lh Lumina-unsigned.ipa

      - uses: actions/upload-artifact@v4
        with:
          name: lumina-unsigned-ipa
          path: ios/Lumina-unsigned.ipa
          retention-days: 14
