# Lumina CI Workflows

The `ci.yml` workflow runs on every push and pull request to ensure code quality and build integrity. It typechecks, lints, and tests all changes, then builds a debug APK for Android. Download the APK from the Actions run's Artifacts tab (lumina-debug-apk).
